ObjetivoHoy hablaremos sobre tipos de datos. ¡Echa un vistazo a la pestaña "Tutorial" para ver materiales de aprendizaje y un video instructivo!TareaCompleta el código en el editor de abajo. Las variables $i$, $d$ y $s$ ya están declaradas e inicializadas por ti. Debes:Declarar 3 variables: una de tipo int (entero), una de tipo double (decimal de doble precisión) y una de tipo String (cadena de texto).Leer 3 líneas de entrada desde stdin (de acuerdo con la secuencia proporcionada en la sección "Formato de Entrada" a continuación) e inicializar tus 3 variables.Utilizar el operador + para realizar las siguientes operaciones:Imprimir la suma de $i$ más tu variable int en una nueva línea.Imprimir la suma de $d$ más tu variable double redondeada a un lugar decimal en una nueva línea.Concatenar $s$ con la cadena de texto que leíste como entrada e imprimir el resultado en una nueva línea.Nota: Si estás utilizando un lenguaje que no admite el uso de + para la concatenación de cadenas (por ejemplo: C), puedes simplemente imprimir una variable inmediatamente después de la otra en la misma línea. La cadena proporcionada en tu editor debe imprimirse primero, seguida inmediatamente por la cadena que leíste como entrada.Formato de EntradaLa primera línea contiene un entero que debes sumar con $i$.La segunda línea contiene un double que debes sumar con $d$.La tercera línea contiene una cadena de texto que debes concatenar con $s$.Formato de SalidaImprime la suma de ambos enteros en la primera línea, la suma de ambos doubles (redondeada a 1 lugar decimal) en la segunda línea, y luego las dos cadenas concatenadas en la tercera línea.Entrada de MuestraPlaintext12
4.0
is the best place to learn and practice coding!
Salida de MuestraPlaintext16
8.0
HackerRank is the best place to learn and practice coding!
ExplicaciónCuando sumamos los enteros $i$ (que ya viene predefinido) y $12$, obtenemos el entero $16$.Cuando sumamos los números de punto flotante $d$ y $4.0$, obtenemos $8.0$.Cuando concatenamos HackerRank  con is the best place to learn and practice coding!, obtenemos HackerRank is the best place to learn and practice coding!.No pasarás este desafío si intentas asignar los valores del caso de muestra directamente a tus variables en lugar de seguir las instrucciones anteriores y leer la entrada desde stdin.